#!/bin/bash

set -e

pushd /home/jlewallen/jlewallen/playlist-generator
if [ -f generator ]; then
	./generator
	echo "copying new playlists"
	rsync -vua *.json ~/dropbox/personal/jacoblewallen.com/content/music
fi
popd

pushd ~/dropbox/personal
echo "uploading site"
make upload
popd
